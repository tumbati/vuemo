function createAppCore(srcDirPath) {
  const appCoreDirs = [
    'http',
    'helpers',
    'guards',
    'config',
    'middleware',
    'services',
    'mixins'
  ]
}

module.exports = createAppCore
