const mountRoot = require('./mount-root')
const createCore = require('./create-core')
const createLayout = require('./create-layout')
const createSRC = require('./create-src')

module.exports = {
  mountRoot,
  createCore,
  createLayout,
  createSRC
}
